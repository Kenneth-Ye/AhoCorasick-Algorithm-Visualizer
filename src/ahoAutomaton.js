const Automaton = ({substrings, mainstring}) => {
    console.log(substrings);
    return(
        <div>
            <h1>{substrings}</h1>
            <h1>{mainstring}</h1>
        </div>
    );
}

export default Automaton;
